"use strict"; // Treat all js code in the document as newer version of js

// alert(3+3) cannot use this since we are in node and not in browser

let name = "harsh" // string
let age = 18 // number
let isLoggedIn = false // boolean

/*so there are these data types as follows
number => it also has a range to overcome it we can use
bigint

string => can either use "" or ''

boolean => either true/false

null => standalone value

undefined => value not assigned

symbol => to find thee uniqueness of components

object*/

console.log(typeof undefined) // undefined
console.log(typeof null) // object